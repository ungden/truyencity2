/**
 * Ad config — DISABLED until app is approved.
 * Re-enable by restoring react-native-google-mobile-ads plugin in app.config.ts
 * and uncommenting the original implementation.
 */

export const AD_UNITS = {
  BANNER_HOME: "disabled",
  BANNER_DETAIL: "disabled",
  INTERSTITIAL_CHAPTER: "disabled",
};

export const INTERSTITIAL_CHAPTER_INTERVAL = 4;
