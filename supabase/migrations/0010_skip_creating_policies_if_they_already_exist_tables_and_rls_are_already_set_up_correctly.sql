-- Tables and policies already exist, skipping to avoid duplicate policy error
-- The notification system is already properly configured in the database
SELECT 'Notification tables and policies already exist' as status;